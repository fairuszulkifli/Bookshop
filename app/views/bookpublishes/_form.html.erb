<%= form_for(@bookpublish) do |f| %>
  <% if @bookpublish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookpublish.errors.count, "error") %> prohibited this bookpublish from being saved:</h2>

      <ul>
      <% @bookpublish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
  <%= f.label :book_id %>
  <div class="col-lg-12"%>
  <%= f.collection_select(:book_id,Book.all,:id,:published_name,{:prompt=>"Please Select"})%>
 </div>
 </div>

 <div class="field">
  <%= f.label :publisher_id %>
  <div class="col-lg-12"%>
  <%= f.collection_select(:publisher_id,Publisher.all,:id,:publisher_name,{:prompt=>"Please Select"})%>
 </div>
 </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
