<%= form_for(@apprentice) do |f| %>
  <% if @apprentice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apprentice.errors.count, "error") %> prohibited this apprentice from being saved:</h2>

      <ul>
      <% @apprentice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
  <%= f.label :author_id %>
  <div class="col-lg-12"%>
  <%= f.collection_select(:author_id,Author.all,:id,:name,{:prompt=>"Please Select"})%>
 </div>
 </div>
 
  <div class="field">
    <%= f.label :apprentice_name %><br>
    <%= f.text_field :apprentice_name %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
